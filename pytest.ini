[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    -p no:warnings
markers =
    unit: 单元测试
    integration: 集成测试
    slow: 慢速测试
    db: 需要数据库的测试
